/*
import House from "./HouseWithBuilderDesignPattern";

let builder = new House.Builder();

let house: House = builder
                        .setTopRoofColor("Red")
                        .setMainDoorNumber(2)
                        .setWindowNumber(10)
                        .setColor("White")
                        .build();

                        */
